version: 2

models:
  - name: stg_rugs_usa_variant
    description: Raw variant product data.
    columns:
      - name: p_id
        tests:
          - not_null
      - name: variant
        tests:
          - not_null